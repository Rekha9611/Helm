apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myworld.fullname" . }}-test-connection"
  labels:
    {{- include "myworld.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myworld.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
