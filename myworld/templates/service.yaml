apiVersion: v1
kind: Service
metadata:
  name: {{ include "myworld.fullname" . }}
  labels:
    {{- include "myworld.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myworld.selectorLabels" . | nindent 4 }}
